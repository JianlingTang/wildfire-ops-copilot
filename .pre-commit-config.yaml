default_install_hook_types: [pre-commit]

repos:
  - repo: local
    hooks:
      - id: wildfire-ops-ci-checks
        name: Wildfire Ops CI checks
        entry: bash scripts/check_ci_local.sh
        language: system
        pass_filenames: false
        stages: [pre-commit]
